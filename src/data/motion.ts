import type { MotionDesign } from '@/types';

/**
 * ─── YOUR MOTION DESIGN FILES ──────────────────────────────────────────────
 *
 * 1. Put the video file in /public/uploads/   (e.g. myproject.mp4)
 * 2. Add an entry below:
 *
 * {
 *   id: 'unique-id',
 *   title: 'Project name',
 *   description: 'Short desc', ← optional
 *   filename: 'myproject.mp4', ← file name only, no path
 *   thumbnail: 'myproject.jpg',← optional still image (also in /public/uploads/)
 *   order: 0,
 * },
 */
export const motionDesigns: MotionDesign[] = [
  // ── paste your motion designs below ─────────────────────────────────────
];
