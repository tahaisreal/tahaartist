import type { YoutubeVideo } from '@/types';

/**
 * ─── YOUR YOUTUBE VIDEOS ───────────────────────────────────────────────────
 *
 * To add a video, copy this block and fill in the fields:
 *
 * {
 *   id: 'unique-id',           ← any unique string (e.g. '1', '2', ...)
 *   title: 'Video title',
 *   description: 'Short desc', ← optional, leave out or set to null
 *   youtubeId: 'dQw4w9WgXcQ', ← the ID from the YouTube URL (?v=XXXX)
 *   order: 0,                  ← display order, lower = first
 * },
 */
export const youtubeVideos: YoutubeVideo[] = [
  // ── paste your videos below ─────────────────────────────────────────────
];
